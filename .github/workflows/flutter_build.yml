name: Build APK

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - uses: actions/setup-java@v4
      with:
        distribution: 'temurin'
        java-version: '17'
        
    - uses: subosito/flutter-action@v2
      with:
        flutter-version: '3.32.1'
        cache: true
        
    - name: Fix build configuration
      run: |
        # 1. Corregir minSdk y NDK en build.gradle.kts
        sed -i 's/minSdk = 21/minSdk = 23/g' android/app/build.gradle.kts
        
        # 2. Asegurar que NDK version está presente
        if ! grep -q "ndkVersion" android/app/build.gradle.kts; then
          sed -i '/compileSdk = 34/a\    ndkVersion = "27.0.12077973"' android/app/build.gradle.kts
        fi
        
        # 3. Corregir versión de Kotlin en settings.gradle.kts (compatible con AGP 8.7.0)
        sed -i 's/org.jetbrains.kotlin.android.*version.*"1.8.22"/org.jetbrains.kotlin.android" version "1.9.10"/g' android/settings.gradle.kts
        
        # 4. Verificar cambios
        echo "✅ Cambios en build.gradle.kts:"
        grep -n "minSdk\|ndkVersion" android/app/build.gradle.kts || echo "No se encontraron las líneas"
        
        echo "✅ Cambios en settings.gradle.kts:"
        grep -n "kotlin.android" android/settings.gradle.kts || echo "No se encontró kotlin"
        
    - name: Build
      run: |
        flutter clean
        flutter pub get
        flutter build apk --release --target-platform android-arm64 --android-skip-build-dependency-validation
        
    - uses: actions/upload-artifact@v4
      with:
        name: apk
        path: build/app/outputs/flutter-apk/app-release.apk